// Redirect new tabs to the Karakeep Dashboard
// Uses the same URL stored in localStorage by the dashboard, or falls back to default
const DASHBOARD_URL = 'http://100.114.3.92/';
window.location.replace(DASHBOARD_URL);
